[project]
name = "the_chicken_farm"
version = "0.1.0"
description = "The Chicken Farm - Restaurant & Franchise Management"
requires-python = ">=3.10"

[build-system]
requires = ["flit_core>=3.2,<4"]
build-backend = "flit_core.buildapi"
